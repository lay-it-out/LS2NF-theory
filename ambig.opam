opam-version: "2.0"
name: "ambig"
synopsis: "Ambiguity theory"

depends: [
  "coq" { (>= "8.13.2" & < "8.14~") }
  "coq-stdpp" { >= "dev" }
  "dune" { >= "2.7.0" }
]

build: [
  ["dune" "subst"] {pinned}
  ["dune" "build" "-p" name "-j" jobs]
]
